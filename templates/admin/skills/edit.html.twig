{% extends 'admin/base.html.twig' %}

{% block title %}Edit Skill{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0">Edit Skill</h1>
        <a href="{{ path('admin_skills') }}" class="btn btn-secondary">Back to list</a>
    </div>

    <div class="card shadow">
        <div class="card-body">
            {{ form_start(form) }}
            <div class="row">
                <div class="col-md-6">
                    {{ form_row(form.name) }}
                    {{ form_row(form.percentage) }}
                    {{ form_row(form.category) }}
                </div>
            </div>

            <div class="mt-3">
                <button type="submit" class="btn btn-primary">Save changes</button>
                <a href="{{ path('admin_skills') }}" class="btn btn-link">Cancel</a>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}