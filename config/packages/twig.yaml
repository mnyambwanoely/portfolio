twig:
    default_path: '%kernel.project_dir%/templates'
    cache: false  # Disable cache to fix template issues
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    auto_reload: true